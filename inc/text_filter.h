#ifndef TEXT_FILTER_H
#define TEXT_FILTER_H

#include <vector>
#include <string>


class TextFilter {
    public:
        std::vector<int> findNumbers(const std::string& text) const;

    private:


};


#endif // TEXT_FILTER_H